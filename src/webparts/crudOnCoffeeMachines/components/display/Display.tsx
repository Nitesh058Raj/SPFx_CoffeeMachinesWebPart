import * as React from "react";
import styles from "./Display.module.scss";

export const Display: React.FC = ({}) => {
	return(
		<>
			<div className={styles.displayContainer}> Display component </div>
		</>
	)
}